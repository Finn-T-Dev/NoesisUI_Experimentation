
<Page
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  mc:Ignorable="d"
  xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
  d:DesignWidth="1920"
  d:DesignHeight="1080"
  Background="#FF4D5052"
  Focusable="True" d:DataContext="ViewModel">
  
  <Page.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="MenuStyles.xaml"/>
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </Page.Resources>
  
  <Grid x:Name="Master">
    <Grid x:Name="Background">
      <Grid.Background>
        <ImageBrush ImageSource="Assets/scifi_main_menu.jpg" Stretch="UniformToFill"/>
      </Grid.Background>
      <Grid x:Name="HeaderBar" Background="#FF000000" Height="32" VerticalAlignment="Top"/>
      <Grid x:Name="FooterBar" Height="32" Background="#FF000000" VerticalAlignment="Bottom"/>
    </Grid>
    <Grid x:Name="Interface" Style="{DynamicResource InterfaceLayout}">
      <Grid.RowDefinitions>
        <RowDefinition Height="64"/>
        <RowDefinition Height="80"/>
        <RowDefinition Height="64"/>
        <RowDefinition Height="838*"/>
        <RowDefinition Height="64"/>
      </Grid.RowDefinitions>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="64"/>
        <ColumnDefinition Width="1*"/>
        <ColumnDefinition Width="2*"/>
        <ColumnDefinition Width="1*"/>
        <ColumnDefinition Width="64"/>
      </Grid.ColumnDefinitions>
      
      <Grid x:Name="TitleArea" Grid.Row="1" Grid.Column="2" Margin="0">
        <TextBlock x:Name="TitleText" Text="Settings" TextWrapping="Wrap" HorizontalAlignment="Center" FontFamily="Broadway" FontSize="56"/>
      </Grid>
      
      <Grid x:Name="SubtitleArea" Grid.Row="2" Grid.Column="2" HorizontalAlignment="Center" Margin="0,12,0,12">
        <TextBlock x:Name="SubtitleText" Text="Configure your experience" TextWrapping="Wrap" FontSize="24"/>
      </Grid>
      
      <StackPanel x:Name="SettingsStack" Grid.Row="3" Grid.Column="2" VerticalAlignment="Top" Margin="10,40,10,0">
        
        <!-- Sound Toggle -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,30">
          <TextBlock Text="SOUND" FontSize="32" Foreground="White" VerticalAlignment="Center" Margin="0,0,40,0"/>
          <CheckBox x:Name="SoundCheckBox" IsChecked="True" VerticalAlignment="Center"/>
        </StackPanel>
        
        <!-- Master Volume -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,30">
          <TextBlock Text="MASTER VOLUME" FontSize="32" Foreground="White" VerticalAlignment="Center" Margin="0,0,40,0"/>
          <Slider x:Name="MasterVolumeSlider" Width="300" Minimum="0" Maximum="1" 
                  Value="{Binding MasterVolume, Mode=TwoWay}" VerticalAlignment="Center"/>
        </StackPanel>
        
        <!-- Back Button -->
        <Button x:Name="BackButton" Content="BACK" Command="{Binding BackCommand}" Style="{DynamicResource SciFiButtonStyle}"/>
        
      </StackPanel>
    </Grid>
  </Grid>
</Page>